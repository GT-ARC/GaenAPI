package garuntimeenv.interfaces;

/**
 * Each element that is a property instance shall implement this interface.
 * In this regard belong genetic operators, selection algos, and the hyper parameter values which are wrapped
 * through the NumberProperty wrapper which extends the Property interface.
 */
public interface Property {

}
